# Promptovi — Lab 4

Ovdje zapisujte sve promptove koje ste koristili za GitHub Copilot.

## Zadatak 2
Write a Python function called get_student_info that takes two parameters: name (str) and year (int). The function should return a dictionary containing the keys "name", "year", and "greeting". The greeting should be a formatted f-string like: "Zdravo Amina, vi ste 3 godina studija".


## Zadatak 4
Generate a Python class named Course with attributes name (str), code (str), and credits (int). Include an __init__ method to initialize these attributes. Also implement a method called description() that returns a formatted string in the following format: "TK207 - Razvoj telekomunikacijske programske podrške (6 kredita)", where the code, name, and credits are dynamically inserted from the object.